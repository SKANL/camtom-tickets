# openspec config — camtom-tickets
# Auto-generated by sdd-init (2026-07-14)

context: |
  Web-based TV dashboard connecting to Linear's GraphQL API (api.linear.app/graphql)
  via personal API key. Shows support tickets grouped by urgency with configurable
  SLA timers, styled like a fast-food order board.
  
  Stack: Node.js / Express backend + static SPA frontend (framework TBD).
  This is a brand-new project — no code, no test runner, no dependencies yet.

strict_tdd: false
  # Test runner not detected (no package.json, no test config found).
  # Will be re-evaluated when the project is scaffolded.

testing:
  runner:
    detected: false
    command: null
    framework: null
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null

phases:
  proposal: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
